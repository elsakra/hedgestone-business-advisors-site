---
---

<section id="home" class="relative min-h-screen flex items-center justify-center overflow-hidden bg-gradient-to-br from-slate-50 to-white">
  <!-- Hero background image -->
  <div class="absolute inset-0 z-0">
    <img 
      src="https://images.unsplash.com/photo-1486406146926-c627a92ad1ab?w=1920&h=1080&fit=crop&q=80" 
      alt="Modern office building"
      class="w-full h-full object-cover opacity-10"
    />
  </div>
  
  <!-- Subtle geometric pattern background -->
  <div class="absolute inset-0 opacity-5">
    <svg class="w-full h-full" xmlns="http://www.w3.org/2000/svg">
      <defs>
        <pattern id="grid" width="40" height="40" patternUnits="userSpaceOnUse">
          <path d="M 40 0 L 0 0 0 40" fill="none" stroke="currentColor" stroke-width="1"/>
        </pattern>
      </defs>
      <rect width="100%" height="100%" fill="url(#grid)" />
    </svg>
  </div>
  
  <!-- Gradient orbs for depth -->
  <div class="absolute top-1/4 -left-1/4 w-[600px] h-[600px] bg-emerald-600/10 rounded-full blur-[120px]"></div>
  <div class="absolute bottom-1/4 -right-1/4 w-[500px] h-[500px] bg-slate-900/5 rounded-full blur-[100px]"></div>
  
  <div class="relative z-10 max-w-7xl mx-auto px-6 lg:px-8 text-center">
    <h1 class="text-5xl md:text-7xl lg:text-8xl font-black tracking-tight text-slate-900 mb-6 animate-on-scroll">
      Strategic Clarity.
      <span class="block text-emerald-600">Decisive Action.</span>
    </h1>
    
    <p class="text-xl md:text-2xl text-slate-600 max-w-3xl mx-auto mb-12 animate-on-scroll">
      Elite business advisory services for leaders ready to transform vision into exceptional value.
    </p>
    
    <div class="flex flex-col sm:flex-row gap-4 justify-center animate-on-scroll">
      <a href="/contact" class="inline-flex items-center gap-3 px-8 py-4 text-lg font-semibold bg-gradient-to-r from-emerald-600 to-emerald-700 text-white rounded-full shadow-lg shadow-emerald-600/25 hover:shadow-xl hover:shadow-emerald-600/30 hover:scale-105 transition-all duration-300">
        Schedule Consultation
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
        </svg>
      </a>
      <a href="#services" class="inline-flex items-center gap-2 px-8 py-4 text-lg font-semibold text-emerald-600 border-2 border-emerald-600 rounded-full hover:bg-emerald-50 hover:scale-105 transition-all duration-300">
        Our Services
      </a>
    </div>
  </div>
  
  <!-- Scroll indicator -->
  <div class="absolute bottom-8 left-1/2 -translate-x-1/2 animate-bounce">
    <svg class="w-6 h-6 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
    </svg>
  </div>
</section>